Add Telegram bot integration for a. Create lib/telegram.py that: (1) uses python-telegram-bot library, (2) receives messages, (3) routes them through a send command, (4) returns response. Add PEP 723 metadata for uv auto-install of python-telegram-bot. Add a.c dispatch entry for "telegram" that calls fallback_py("telegram"...). Keep it minimal — ~50-100 lines. Read lib/email.py and lib/net.c for patterns. When done run: a done "added telegram bot integration"