#!/bin/bash
# Fast interactive picker (<1ms startup) - reads cache, uses fzf
CACHE=~/.local/share/aios/i_cache.txt
[ -f "$CACHE" ] || python3 "$(dirname "$0")/aio.py" i >/dev/null 2>&1
sel=$(fzf --height=10 --reverse < "$CACHE") && exec aio $sel
