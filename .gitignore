# Secrets & credentials
*.env
.env*
*credentials*
*secret*
*token*

# Python cache
__pycache__/
*.pyc
*.pyo

# Databases
*.db
*.sqlite
*.sqlite3

# Local config
.local/

.aider*
.a_job
/my

# User data — adata/ is a separate world from the code repo.
# adata/git/ is its own independent git repo (not a submodule).
# .gitignore makes the parent repo completely blind to it.
adata/

# C build artifacts
*.o
*.plist
a
